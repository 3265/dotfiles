# -------
# tmux.conf
# -------

# generarl
source ~/dotfiles/.config/tmux/.tmux.conf.general

# message
source ~/dotfiles/.config/tmux/.tmux.conf.message

# statusbar
source ~/dotfiles/.config/tmux/.tmux.conf.statusbar

# window
source ~/dotfiles/.config/tmux/.tmux.conf.window

# pane
source ~/dotfiles/.config/tmux/.tmux.conf.pane

# copy
source ~/dotfiles/.config/tmux/.tmux.conf.copy

# keybinding
source ~/dotfiles/.config/tmux/.tmux.conf.keybinding

# plugin
source ~/dotfiles/.config/tmux/.tmux.conf.plugin

# tmux-colors-solarizedでpaneのボーダが見づらいため
set -g pane-border-style fg=green
