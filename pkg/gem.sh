#!/bin/bash

# ruby env
# sudo apt-get install rbenv ruby-build
# it is better to install rbenv from source code instead of using apt

# Pkgs
gem install rails
sudo apt-get install libmysqld-dev # for gem install mysql2
# rbenv reflesh
