# dotfiles

## Repository Layout

- `config/`: config files linked into `$HOME` or `~/.config`, or managed by Home Manager
- `setup/`: Debian/Ubuntu setup scripts
- `assets/`: non-runtime assets such as wallpapers and QMK keymaps

