#!/bin/bash

pipx install poetry
pipx install pipenv
pipx install pip-tools

pipx install black
pipx install isort
pipx install mypy
pipx install flake8
pipx install autoflake
pipx install pytest

pipx install codemod
pipx install pycowsay
